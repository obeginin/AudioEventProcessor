# 🧠 AudioEventProcessor — асинхронный сервис анализа аудиозаписей на наличие крика и перебиваний собеседников
Этот проект представляет собой потоковый сервис для автоматизированного анализа аудиозаписей.  
Сервис выполняет обработку большого количества файлов, подключается к внешнему хранилищу, извлекает аудиозаписи по заданным условиям и анализирует их на наличие голосовых событий.

## 🎯 Основные возможности

- Поиск и загрузка аудиофайлов из Linux-хранилища или удалённого сервиса.
- Асинхронная обработка файлов с управлением параллелизмом.
- Анализ аудио на основе:
  - спектрограммы,
  - амплитудно-частотных характеристик (АЧХ),
  - уровня громкости.
- Детекция голосовых событий:
  - резкие возгласы / крики,
  - перебивания собеседников.
- Логирование результатов в формате JSON.
- Экспорт обработанных данных в Excel.
- Поддержка устойчивой работы с большими объёмами данных  
  (до **миллиона файлов за один потоковый цикл**).

## 🧩 Архитектура

Сервис состоит из нескольких основных модулей:

- **Модуль поиска файлов** — получает список доступных аудиозаписей по заданному фильтру (например, по дате).
- **Модуль загрузки** — взаимодействует по API со сторонним сервисом для получения аудио.
- **Модуль анализа** — обрабатывает аудио и определяет наличие событий.
- **Модуль логирования** — сохраняет результаты анализа и формирует итоговые отчёты.

Все операции выполняются асинхронно, что позволяет эффективно обрабатывать большие объёмы данных без снижения производительности.


## 🛠 Технологический стек

- **Python 3.10+**
- **Asyncio** — асинхронная обработка, управление конкурентностью
- **httpx** — асинхронные HTTP-запросы
- **aiofiles** — асинхронная работа с файловой системой
- **NumPy / SciPy** — анализ аудиосигналов
- **Librosa** или **pydub** — извлечение признаков, работа с аудио
- **ProcessPoolExecutor / ThreadPoolExecutor** — параллельная обработка аудиофайлов
- **JSON / CSV / Excel (pandas, openpyxl)** — логирование и формирование итоговых отчётов
- **Linux-хранилище (NFS/Samba/локальный storage)** — источник аудиофайлов
- **Docker (опционально)** — контейнеризация сервиса

